import React from 'react';
import ReacDom from 'react-dom/client';

const Qmore = ()=>{
  return (<form>
    <button onClick={()=>{}}>MORE ANSWERED QUESTIONS</button>
  </form>);
};

export default Qmore;